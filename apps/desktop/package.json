{
  "name": "@agent/desktop",
  "private": true,
  "version": "0.0.0",
  "description": "Electron companion for the Gemini Live desktop agent.",
  "author": "jongwoo",
  "type": "module",
  "main": "./main.js",
  "scripts": {
    "dev": "npm --prefix ../.. run build:runtimes && electron .",
    "clean:dist": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "dist:dir": "npm run clean:dist && npm --prefix ../.. run build:runtimes && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --dir --mac --universal",
    "dist:mac": "npm run clean:dist && npm --prefix ../.. run build:runtimes && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac zip --universal",
    "dist:mac:x64": "npm run clean:dist && npm --prefix ../.. run build:runtimes && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac zip --x64",
    "dist:win": "npm run clean:dist && npm --prefix ../.. run build:runtimes && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --win nsis --x64"
  },
  "dependencies": {
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "electron": "^40.8.0",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "dev.jongwoo.gemini.liveagent",
    "productName": "Desktop Companion",
    "electronVersion": "40.8.0",
    "asar": true,
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.cjs",
      "renderer/**/*",
      "src/main/cloud/**/*",
      "src/main/config/**/*",
      "src/main/debug/**/*",
      "src/main/execution/**/*",
      "src/main/ipc/**/*",
      "src/main/session/hosted-session-runtime.js",
      "src/main/ui/**/*",
      "package.json"
    ],
    "mac": {
      "identity": null,
      "target": [
        {
          "target": "zip",
          "arch": [
            "universal"
          ]
        }
      ],
      "category": "public.app-category.productivity"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
